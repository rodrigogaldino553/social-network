<!DOCTYPE html>
<html style="min-height: 100vh; height: max-content;">
  <head>
    <title>SocialNetwork</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Post your pictures and what you thinking">
    <meta name="keywords" content="meet people, network, online meeting">
    <meta name="author" content="Rodrigo Galdino">

    <%= favicon_link_tag asset_path('favicon.png') %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <% if content_for?(:head) %>
      <%= yield :head %>
    <% end %>

    <%= render 'layouts/google_analytics' %>
  </head>

  <body style="min-height: 100vh; height: max-content;">
    <%= render 'shared/alert' %>
    <div style="min-height: 100vh; height: max-content;">
      <%= render 'shared/header' %>
      <div class="mt-2 mx-auto d-flex flex-column align-middle">
        <%= yield %>
      </div>
    </div>
    </div>
    <%= render 'shared/footer' %>
  </body>
</html>
